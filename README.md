# FreshMart POS split files

The pasted React code has been arranged into separate files under `src/`.

- `src/App.jsx` keeps the main app shell and imports `./App.css`.
- `src/components/` contains shared UI pieces.
- `src/screens/` contains each app screen.
- `src/utils/` contains storage and formatting helpers.
